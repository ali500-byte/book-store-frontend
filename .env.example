# Copy this file to .env for local development
VITE_API_URL=http://localhost:5000/api

# Production (Azure Static Web App → Environment variables):
# VITE_API_URL=https://your-app-service.azurewebsites.net/api
# Used at build time to proxy /api/* to your App Service (see scripts/write-swa-config.mjs)
