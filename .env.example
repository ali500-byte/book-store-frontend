# Local development — copy to .env
VITE_API_URL=http://localhost:5000/api

# Production URL is in .env.production (used by vite build).
# Azure Static Web App environment variable VITE_API_URL overrides it if set.
